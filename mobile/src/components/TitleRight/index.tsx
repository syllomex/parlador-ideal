import React from "react";

import { BottomLine, Container, TopLine } from "./styles";

const TitleRight: React.FC = () => {
  return (
    <Container>
      <TopLine>Parlador</TopLine>
      <BottomLine>Ideal</BottomLine>
    </Container>
  );
};

export default TitleRight;
